<UserControl
    x:Class="Wpf.CartesianChart.Labels.LabelsHorizontalExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.CartesianChart.Labels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock>Labels</TextBlock>
        <Button
            Grid.Row="1" Height="30" Click="UpdateAllOnClick">
            Move All
        </Button>
        <wpf:CartesianChart Grid.Row="2" Series="{Binding SeriesCollection}">
            <wpf:CartesianChart.DataTooltip>
                <wpf:DefaultTooltip SelectionMode="SharedYValues" />
            </wpf:CartesianChart.DataTooltip>
            <wpf:CartesianChart.AxisX>
                <wpf:Axis Title="Sold Items" LabelFormatter="{Binding Formatter}" />
            </wpf:CartesianChart.AxisX>
            <wpf:CartesianChart.AxisY>
                <wpf:Axis Labels="{Binding Labels}" LabelsRotation="65">
                    <wpf:Axis.Separator>
                        <wpf:Separator Step="1" />
                    </wpf:Axis.Separator>
                </wpf:Axis>
            </wpf:CartesianChart.AxisY>
        </wpf:CartesianChart>
    </Grid>
</UserControl>
