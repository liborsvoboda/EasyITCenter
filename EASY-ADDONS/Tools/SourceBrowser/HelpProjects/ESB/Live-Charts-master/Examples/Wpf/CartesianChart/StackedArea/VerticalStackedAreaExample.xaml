<UserControl
    x:Class="Wpf.CartesianChart.StackedArea.VerticalStackedAreaExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.CartesianChart.StackedArea"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" TextWrapping="Wrap">
            <Bold>New in 0.7.0</Bold>
            use VerticalStackedAreaSeries or StackedAreaSeries classes, depending on your needs, this series also supports 2 different stack modes: Percentage and Values<LineBreak />
        </TextBlock>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button
                Width="100" Height="30" Click="UpdateAllOnClick">
                Move All
            </Button>
            <Button
                Width="120" Height="30" Margin="10,0" Click="ChangeStackModeOnClick">
                Change StackMode
            </Button>
        </StackPanel>
        <lvc:CartesianChart
            Grid.Row="2" LegendLocation="Right"
            Series="{Binding SeriesCollection}" />
    </Grid>
</UserControl>
