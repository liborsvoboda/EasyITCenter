<UserControl
    x:Class="Wpf.CartesianChart.ThreadSafe.ThreadSafeExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:threadSafe="clr-namespace:Wpf.CartesianChart.ThreadSafe"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <threadSafe:ThreadSafeViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Command="{Binding ReadCommand}">Start Lecture</Button>
        <lvc:CartesianChart
            Grid.Row="1"
            DataTooltip="{x:Null}"
            DisableAnimations="True">
            <lvc:CartesianChart.Series>
                <lvc:LineSeries
                    LineSmoothness="0"
                    PointGeometry="{x:Null}"
                    Values="{Binding Values}" />
            </lvc:CartesianChart.Series>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis MinValue="{Binding Min}" />
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>
    </Grid>
</UserControl>



